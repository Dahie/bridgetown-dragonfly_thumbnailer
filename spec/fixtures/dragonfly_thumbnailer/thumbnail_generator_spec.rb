# frozen_string_literal: true

require "spec_helper"

describe(DragonflyThumbnailer::ThumbnailGenerator) do


  xit "outputs the sample Liquid tag" do
    expect(contents).to match "This plugin works!"
  end
end
